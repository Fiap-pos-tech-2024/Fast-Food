import { dataBaseInterface } from "../../domain/interface/database";
import { orderRepository } from "../../domain/interface/orderRepository";

export class dataBase implements dataBaseInterface {

    async connectionDB() {

    }
}